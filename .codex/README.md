# Codex reads ~/.codex/config.toml (no project-scope MCP servers).
# safari-mcp is registered there via: codex mcp add safari-mcp -- "/Applications/Safari Technology Preview.app/Contents/MacOS/safaridriver" --mcp
# See AGENTS.md.
